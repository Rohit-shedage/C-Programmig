#include<stdio.h>
#include<conio.h>

int main()
{

    int Arr[5]={};

    printf("\n Element of 1st Element is => %d.",Arr[0]);
    printf("\n Element of 2nd Element is => %d.",Arr[1]);
    printf("\n Element of 3rd Element is => %d.",Arr[2]);
    printf("\n Element of 4th Element is => %d.",Arr[3]);
    printf("\n Element of 5th Element is => %d.",Arr[4]);

    getch();
    return 0;
}
